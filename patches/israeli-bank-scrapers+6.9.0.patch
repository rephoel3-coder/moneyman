diff --git a/node_modules/israeli-bank-scrapers/lib/helpers/elements-interactions.js b/node_modules/israeli-bank-scrapers/lib/helpers/elements-interactions.js
index efd9183..0111eae 100644
--- a/node_modules/israeli-bank-scrapers/lib/helpers/elements-interactions.js
+++ b/node_modules/israeli-bank-scrapers/lib/helpers/elements-interactions.js
@@ -45,7 +45,16 @@ async function fillInput(pageOrFrame, inputSelector, inputValue) {
     // @ts-ignore
     inputElement.value = '';
   });
-  await pageOrFrame.type(inputSelector, inputValue);
+  // Typing the whole value in one call (or with no/zero delay) sends every
+  // keystroke back-to-back with no human-scale timing variance, which some
+  // sites' bot-detection (e.g. Radware Bot Manager) uses to flag and
+  // redirect the session mid-form-fill, before the login is even submitted.
+  // A per-character randomized delay is enough to break that specific
+  // signal without changing what actually gets typed.
+  await pageOrFrame.click(inputSelector).catch(() => {});
+  for (const char of inputValue) {
+    await pageOrFrame.type(inputSelector, char, { delay: 60 + Math.floor(Math.random() * 90) });
+  }
 }
 async function setValue(pageOrFrame, inputSelector, inputValue) {
   await pageOrFrame.$eval(inputSelector, (input, value) => {
diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js b/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
index 4e821fa..7c258b5 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
@@ -60,6 +60,7 @@ function createLoginFields(credentials) {
 function getAmountData(amountStr) {
   let amountStrCopy = amountStr.replace(_constants.SHEKEL_CURRENCY_SYMBOL, '');
   amountStrCopy = amountStrCopy.replaceAll(',', '');
+  amountStrCopy = amountStrCopy.replace(/\s+/g, '');
   return parseFloat(amountStrCopy);
 }
 function getTxnAmount(txn) {
@@ -168,7 +169,7 @@ async function isNoTransactionInDateRangeError(page) {
 }
 async function searchByDates(page, startDate) {
   await (0, _elementsInteractions.clickButton)(page, 'a#tabHeader4');
-  await (0, _elementsInteractions.waitUntilElementFound)(page, 'div#fibi_dates');
+  await waitForSelectorResilientToNavigation(page, 'div#fibi_dates', false);
   await (0, _elementsInteractions.fillInput)(page, 'input#fromDate', startDate.format(DATE_FORMAT));
   await (0, _elementsInteractions.clickButton)(page, `button[class*=${CLOSE_SEARCH_BY_DATES_BUTTON_CLASS}]`);
   await (0, _elementsInteractions.clickButton)(page, `input[value=${SHOW_SEARCH_BY_DATES_BUTTON_VALUE}]`);
@@ -176,7 +177,7 @@ async function searchByDates(page, startDate) {
 }
 async function getAccountNumber(page) {
   // Wait until the account number element is present in the DOM
-  await (0, _elementsInteractions.waitUntilElementFound)(page, ACCOUNTS_NUMBER, true, ELEMENT_RENDER_TIMEOUT_MS);
+  await waitForSelectorResilientToNavigation(page, ACCOUNTS_NUMBER, true, ELEMENT_RENDER_TIMEOUT_MS);
   const selectedSnifAccount = await page.$eval(ACCOUNTS_NUMBER, option => {
     return option.innerText;
   });
@@ -208,7 +209,7 @@ async function scrapeTransactions(page, tableLocator, transactionStatus, needToP
   return convertTransactions(txns, options);
 }
 async function getAccountTransactions(page, options) {
-  await Promise.race([(0, _elementsInteractions.waitUntilElementFound)(page, "div[id*='divTable']", false), (0, _elementsInteractions.waitUntilElementFound)(page, `.${ERROR_MESSAGE_CLASS}`, false)]);
+  await Promise.race([waitForSelectorResilientToNavigation(page, "div[id*='divTable']", false), waitForSelectorResilientToNavigation(page, `.${ERROR_MESSAGE_CLASS}`, false)]);
   const noTransactionInRangeError = await isNoTransactionInDateRangeError(page);
   if (noTransactionInRangeError) {
     return [];
@@ -220,7 +221,7 @@ async function getAccountTransactions(page, options) {
 }
 async function getCurrentBalance(page) {
   // Wait for the balance element to appear and be visible
-  await (0, _elementsInteractions.waitUntilElementFound)(page, CURRENT_BALANCE, true, ELEMENT_RENDER_TIMEOUT_MS);
+  await waitForSelectorResilientToNavigation(page, CURRENT_BALANCE, true, ELEMENT_RENDER_TIMEOUT_MS);
 
   // Extract text content
   const balanceStr = await page.$eval(CURRENT_BALANCE, el => {
@@ -228,15 +229,63 @@ async function getCurrentBalance(page) {
   });
   return getAmountData(balanceStr);
 }
+async function waitForSelectorResilientToNavigation(page, selector, onlyVisible, overallTimeoutMs = 60000) {
+  const deadline = Date.now() + overallTimeoutMs;
+  for (;;) {
+    const remaining = deadline - Date.now();
+    if (remaining <= 0) {
+      throw new Error(`waiting for selector \`${selector}\` failed: timeout ${overallTimeoutMs}ms exceeded`);
+    }
+    try {
+      await (0, _elementsInteractions.waitUntilElementFound)(page, selector, onlyVisible, Math.min(remaining, 5000));
+      return;
+    } catch (e) {
+      const msg = e && e.message || '';
+      // A post-login redirect chain can destroy the page's execution context or
+      // detach the node mid-poll. That's a normal part of navigating to the
+      // logged-in page, not a real failure - keep polling until the overall
+      // deadline instead of letting one transient rejection abort the whole race.
+      if (msg.includes('Execution context was destroyed') || msg.includes('Node is detached from document') || msg.includes('waiting for selector')) {
+        continue;
+      }
+      throw e;
+    }
+  }
+}
+async function waitForUrlMatchingSuccess(page, overallTimeoutMs = 60000) {
+  const successPatterns = getPossibleLoginResults()[_baseScraperWithBrowser.LoginResults.Success];
+  const deadline = Date.now() + overallTimeoutMs;
+  for (;;) {
+    if (Date.now() >= deadline) {
+      throw new Error(`waiting for a known post-login URL failed: timeout ${overallTimeoutMs}ms exceeded`);
+    }
+    try {
+      const url = page.url();
+      if (successPatterns.some(pattern => pattern.test(url))) {
+        return;
+      }
+    } catch (e) {
+      // Page mid-navigation; ignore and retry below.
+    }
+    await new Promise(resolve => setTimeout(resolve, 250));
+  }
+}
 async function waitForPostLogin(page) {
-  return Promise.race([(0, _elementsInteractions.waitUntilElementFound)(page, '#card-header', false),
+  return Promise.race([waitForSelectorResilientToNavigation(page, '#card-header', false),
   // New UI
-  (0, _elementsInteractions.waitUntilElementFound)(page, '#account_num', true),
+  waitForSelectorResilientToNavigation(page, '#account_num', true),
   // New UI
-  (0, _elementsInteractions.waitUntilElementFound)(page, '#matafLogoutLink', true),
+  waitForSelectorResilientToNavigation(page, '#matafLogoutLink', true),
   // Old UI
-  (0, _elementsInteractions.waitUntilElementFound)(page, '#validationMsg', true) // Old UI
-  ]);
+  waitForSelectorResilientToNavigation(page, '#validationMsg', true),
+  // Old UI
+  // Newer "appsng" Angular shell doesn't have any of the four DOM markers
+  // above at all - a real production run landed on a genuinely successful,
+  // fully-loaded post-login page (matching the URL patterns already used
+  // to detect login success elsewhere) and just sat there polling for
+  // elements that don't exist in this UI variant until the full timeout.
+  // Recognizing arrival at a known-success URL directly avoids that.
+  waitForUrlMatchingSuccess(page)]);
 }
 async function fetchAccountData(page, startDate, options) {
   const accountNumber = await getAccountNumber(page);
@@ -250,12 +299,26 @@ async function fetchAccountData(page, startDate, options) {
   };
 }
 async function getAccountIdsOldUI(page) {
-  return page.evaluate(() => {
-    const selectElement = document.getElementById('account_num_select');
-    const options = selectElement ? selectElement.querySelectorAll('option') : [];
-    if (!options) return [];
-    return Array.from(options, option => option.value);
-  });
+  const deadline = Date.now() + 20000;
+  for (;;) {
+    try {
+      return await page.evaluate(() => {
+        const selectElement = document.getElementById('account_num_select');
+        const options = selectElement ? selectElement.querySelectorAll('option') : [];
+        if (!options) return [];
+        return Array.from(options, option => option.value);
+      });
+    } catch (e) {
+      const msg = e && e.message || '';
+      // Reached right after navigating to TRANSACTIONS_URL, where a lingering
+      // redirect can still tear down the execution context mid-evaluate -
+      // that's not a real failure, so retry until the page settles.
+      if ((msg.includes('Execution context was destroyed') || msg.includes('Node is detached from document')) && Date.now() < deadline) {
+        continue;
+      }
+      throw e;
+    }
+  }
 }
 
 /**
diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/base-scraper-with-browser.js b/node_modules/israeli-bank-scrapers/lib/scrapers/base-scraper-with-browser.js
index 4c13609..633a91c 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/base-scraper-with-browser.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/base-scraper-with-browser.js
@@ -13,6 +13,20 @@ var _baseScraper = require("./base-scraper");
 var _errors = require("./errors");
 function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
 const debug = (0, _debug.getDebug)('base-scraper-with-browser');
+// Temporary diagnostic: the failure screenshot for pagi never shows up as a
+// CI artifact even after fixing an unrelated cleanup bug that deleted it -
+// this writes success/failure of the screenshot attempt itself straight to
+// the visible public log (bypassing DEBUG/redirection) to see which it is.
+function _publicLog(msg) {
+  try {
+    if (process.env.MONEYMAN_UNSAFE_STDOUT === 'true') {
+      console.log(msg);
+      return;
+    }
+    const fd = parseInt(process.env.MONEYMAN_PUBLIC_LOG_FD || '3', 10);
+    require('fs').writeSync(fd, msg + '\n');
+  } catch (e) {}
+}
 var LoginBaseResults = /*#__PURE__*/function (LoginBaseResults) {
   LoginBaseResults["Success"] = "SUCCESS";
   LoginBaseResults["UnknownError"] = "UNKNOWN_ERROR";
@@ -53,6 +67,25 @@ async function getKeyByValue(object, value, page) {
   }
   return Promise.resolve(LoginResults.UnknownError);
 }
+async function getCurrentUrlResilientToNavigation(page, clientSide, overallTimeoutMs = 20000) {
+  const deadline = Date.now() + overallTimeoutMs;
+  for (;;) {
+    try {
+      return await (0, _navigation.getCurrentUrl)(page, clientSide);
+    } catch (e) {
+      const msg = e && e.message || '';
+      // Right after submitting the login form, the page may still be mid a
+      // multi-hop redirect. Reading the URL at that exact instant can throw
+      // "Execution context was destroyed" / "Node is detached from document" -
+      // that's a normal side effect of the navigation still happening, not a
+      // real failure, so retry until the page settles or the deadline passes.
+      if ((msg.includes('Execution context was destroyed') || msg.includes('Node is detached from document')) && Date.now() < deadline) {
+        continue;
+      }
+      throw e;
+    }
+  }
+}
 function createGeneralError() {
   return {
     success: false,
@@ -240,7 +273,7 @@ class BaseScraperWithBrowser extends _baseScraper.BaseScraper {
       await (0, _navigation.waitForNavigation)(this.page);
     }
     debug('check login result');
-    const current = await (0, _navigation.getCurrentUrl)(this.page, true);
+    const current = await getCurrentUrlResilientToNavigation(this.page, true);
     const loginResult = await getKeyByValue(loginOptions.possibleResults, current, this.page);
     debug(`handle login results ${loginResult}`);
     return this.handleLoginResult(loginResult);
@@ -250,10 +283,44 @@ class BaseScraperWithBrowser extends _baseScraper.BaseScraper {
     this.emitProgress(_definitions.ScraperProgressTypes.Terminating);
     if (!_success && !!this.options.storeFailureScreenShotPath) {
       debug(`create a snapshot before terminated in ${this.options.storeFailureScreenShotPath}`);
-      await this.page.screenshot({
-        path: this.options.storeFailureScreenShotPath,
-        fullPage: true
-      });
+      // Temporary diagnostic: the screenshot file writes successfully inside
+      // the container but never shows up on the host as a CI artifact (a
+      // separate Docker-on-Windows bind-mount issue) - logging the page's
+      // title/URL/a text snippet directly sidesteps that entirely, since it
+      // travels through the same log pipe the rest of this output already
+      // uses successfully.
+      try {
+        const pageUrl = this.page.url();
+        const pageTitle = await this.page.title();
+        const bodyText = await this.page.evaluate(() => document.body ? document.body.innerText.slice(0, 500) : '(no body)');
+        _publicLog(`page state at failure - url: ${pageUrl} | title: ${pageTitle} | body snippet: ${JSON.stringify(bodyText)}`);
+      } catch (e) {
+        _publicLog(`failed to capture page state at failure: ${e && e.message}`);
+      }
+      // A page that failed mid-navigation is often still unstable right
+      // here, so a bare screenshot() can throw the same transient
+      // "Execution context was destroyed" error the original failure did -
+      // silently losing the screenshot AND overwriting the real error with
+      // this one. Retry through that instead of letting it mask anything.
+      const screenshotDeadline = Date.now() + 15000;
+      for (;;) {
+        try {
+          await this.page.screenshot({
+            path: this.options.storeFailureScreenShotPath,
+            fullPage: true
+          });
+          _publicLog(`screenshot captured at ${this.options.storeFailureScreenShotPath}`);
+          break;
+        } catch (e) {
+          const msg = e && e.message || '';
+          if ((msg.includes('Execution context was destroyed') || msg.includes('Node is detached from document')) && Date.now() < screenshotDeadline) {
+            continue;
+          }
+          debug(`failed to capture failure screenshot: ${msg}`);
+          _publicLog(`failed to capture failure screenshot: ${msg}`);
+          break;
+        }
+      }
     }
     await Promise.all(this.cleanups.reverse().map(safeCleanup));
     this.cleanups = [];
diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/max.js b/node_modules/israeli-bank-scrapers/lib/scrapers/max.js
index 901eba8..e49a683 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/max.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/max.js
@@ -131,6 +131,9 @@ function getTransactionType(planName, planTypeId) {
     case MaxPlanName.Credit:
     case MaxPlanName.CreditOutsideTheLimit:
       return _transactions2.TransactionTypes.Installments;
+    case "הלוואה ברגע למולטי":
+    case "הלוואה בהטבה":
+      return _transactions2.TransactionTypes.Installments;
     default:
       switch (planTypeId) {
         case 2:
